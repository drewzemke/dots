# keyboard repeat rate
# first number is delay, second is rate
xset r rate 300 80

# caps lock remapping
setxkbmap us -option 'caps:ctrl_modifier'
xcape -e 'Caps_Lock=Escape'

# gets the mouse cursor out of the way
unclutter &
